DATABASE_URL=postgresql://catalogi:pg_password@localhost:5432/db
API_PORT=3084
EXTERNAL_SOFTWARE_DATA_ORIGIN=wikidata

# Optional one-time bootstrap for the first Catalogi administrator. This must
# match the email returned by OIDC; remove it after that user has signed in.
# CATALOGI_INITIAL_ADMIN_EMAIL=admin@example.com

# Environment identifier for environment-specific configuration
# Valid values: "local", "dev", "staging", "pre-production", "production"
# Can be used for environment-specific features like Sentry monitoring, logging levels, etc.
# Defaults to "local" if not provided
ENVIRONMENT=local

# Sentry error tracking (optional)
# If not provided, the application will work normally without Sentry integration
# SENTRY_DSN_API=https://your-api-dsn@sentry.example.com/project-id
# SENTRY_DSN_WEB=https://your-web-dsn@sentry.example.com/project-id

VITE_HEAD="
  <title>Catalogi - Local use</title>

  <script defer>
    console.log('This is a custom code in head, used in local');
  </script>
"

# The following variables are use to configure the docker-compose.ressources.yml

# make sure it is aligned with DATABASE_URL, the following are the defaults:

# POSTGRES_DB=db
# POSTGRES_USER=catalogi
# POSTGRES_PASSWORD=pg_password
